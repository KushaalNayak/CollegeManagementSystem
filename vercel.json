{
  "version": 2,
  "builds": [
    {
      "src": "backend/pom.xml",
      "use": "@vercel/java",
      "config": {
        "installCommand": "curl -s https://get.sdkman.io | bash && source ~/.sdkman/bin/sdkman-init.sh && sdk install maven 3.9.6 && mvn dependency:resolve"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "env": {
    "JWT_SECRET": "mySecretKey123456789012345678901234567890"
  }
}
